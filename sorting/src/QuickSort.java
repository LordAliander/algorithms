
/*
 * Select a pivot point (right of the array)
 * move all the numbers smaller than the pivot to the left and all the numbers bigger to the right
 * split the array in half at the new index of the pivot
 */

public class QuickSort {

    static int[] quickSort(int[] array) {

        return array;
    }

    static int[] sort(int[] array) {
        return quickSort(array);
    }
}
